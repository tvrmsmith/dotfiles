run:
  id: "01RUNPROT"
  branch: slice/demo-1
  status: running
  head: abcdef12
  head_sha: abcdef1234567890abcdef1234567890abcdef12
  findings: 1 awaiting
  steps[1]{step,status,findings,duration_ms}:
    lint,awaiting_approval,1,0
gate:
  step: lint
  status: awaiting_approval
  summary: Automatic commit refused for a protected path
  findings[1]{id,severity,file,action,description}:
    protected-path-refusal,error,package.lock,ask-user,"refusing automatic commit: dirty protected path \"package.lock\" matches protected_paths rule \"*.lock\"; index and worktree preserved, inspect and resolve the edit before retrying"
help[6]: Protected-path refusals require an explicit operator response; Approve is rejected.,"Have the operator inspect and resolve the reported protected-path edit through the repository's authorized workflow, then run `no-mistakes axi respond --action fix` to retry the refused step, including its commit and publication.",Run `no-mistakes axi respond --action skip` to skip this step,Run `no-mistakes axi logs --step lint --full` to read the full step log,"A long-running call is working, not stalled - background it if your harness needs to, but the run never advances past a gate on its own. Read every return; on a `gate:`, respond; loop until an `outcome:`.","Commit post-pipeline follow-up work on top of the existing branch so every pipeline fix commit remains present. Never abort-and-restart, reset, or replace the branch in a way that drops prior gate-fix commits."
