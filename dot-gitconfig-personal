[user]
	name = Trevor Smith
	email = tvrmsmith@gmail.com
	# Local signing key file, same rationale as dot-gitconfig's work key: no
	# 1Password agent round trip, so no approval prompt per commit.
	signingkey = ~/.ssh/git-sign-personal
